<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>br.edu.ifma.labmanager</groupId>
        <artifactId>labmanager</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>architecture-metrics</artifactId>
    <name>architecture-metrics</name>
    <description>Instrumento de medição de acoplamento (ArchUnit) — Dimensão 1</description>

    <!-- Dependências para ordenar o reactor Maven (classes lidas de target/classes). -->
    <dependencies>
        <dependency>
            <groupId>br.edu.ifma.labmanager</groupId>
            <artifactId>scheduling-clean-domain</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>br.edu.ifma.labmanager</groupId>
            <artifactId>scheduling-clean-application</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>br.edu.ifma.labmanager</groupId>
            <artifactId>scheduling-clean-presentation</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>br.edu.ifma.labmanager</groupId>
            <artifactId>scheduling-clean-infra</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
